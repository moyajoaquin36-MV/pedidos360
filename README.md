# Pedidos 360 — DSY1107 Desarrollo Cloud Native I (EP1)

| Carpeta | Contenido |
|---|---|
| `frontend/` | Angular 18 + MSAL (login Azure AD, guard, interceptor, roles) |
| `backend/` | 2 microservicios Spring Boot: `ms-pedidos` y `ms-productos`, cada uno con su base de datos |

Aplicacion desplegada: https://ng3zai38m7.execute-api.us-east-1.amazonaws.com
(AWS API Gateway con JWT Authorizer + CORS, EC2 con Docker Compose, Azure AD como IDaaS).
